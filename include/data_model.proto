syntax = "proto2";

package rsdb;

enum Type {
  SET = 1;
  DEL = 2;
}

message KV {
  required bytes key = 1;
  required bytes value = 2;
}

message Flush {
  required Type type = 1;
  optional KV kv = 2;
}
