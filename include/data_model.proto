syntax = "proto2";

package rsdb;

enum Type {
  SET = 1;
  DEL = 2;
}

message Del {
  required bytes key = 1;
}

message Set {
  required bytes key = 1;
  required bytes value = 2;
}

message Flush {
  required Type type = 1;
  optional Set set = 2;
  optional Del del = 3;
}
