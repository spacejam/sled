[package]
name = "tests"
version = "0.1.0"
authors = ["Tyler Neely <t@jujit.su>"]

[dev-dependencies]
sled = { path = "../sled" }
pagecache = { path = "../pagecache" }
crossbeam-epoch = "0.1"
quickcheck = "0.6"
rand = "0.4"
libc = "0.2"
fail = "0.2"
lazy_static = "1.0"
