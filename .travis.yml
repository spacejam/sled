language: rust

rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly
  include:
    - rust: nightly
      env: CLIPPY=true

script:
  - [ -n $CLIPPY ] && cargo test --features="clippy" || cargo test

os:
  - linux
  - osx
